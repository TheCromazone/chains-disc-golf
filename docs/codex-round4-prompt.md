# Codex round four — canopy volume, world cohesion, phone numbers

Paste everything between the rules into Codex in the game folder.

---

You are continuing Chains. Round three is verified: twelve painted tiles, thirty authored clips with a real left-handed set, Lite at 650 KB, every test green, and your own critics still pick Switch Sports golf on one named gap. This round closes that gap and nothing else new.

Start by committing the uncommitted working tree: a ?fps=1 frame-rate readout in src/main.js so a phone can report its own numbers.

One piece, one gauntlet loop. Your final canopy critic said the trees still read as an assembly of similar spherical lobes rather than a cohesive leafy mass. Fix the volume, not the tile: build pine and deciduous crowns in Blender as a few large irregular shells with shaded overlap and a darker interior, bake per-vertex ambient occlusion into vertex colour so the toon ramp shows depth between lobes, vary silhouettes across three variants per species, and keep instancing, the wind bend, the 64 m spatial groups and the collision radii exactly as they are. Judge blind against Switch Sports golf tree lines at phone size from the tee camera and the flyover, and loop until the critic picks ours or names a gap that is not canopy. Keep the terrain, palette and Lite budget unchanged.

Then run the real-device measurement the moment the user hands you numbers: they will open the game on Android and iPhone with ?fps=1 and read the overlay in Lite and Full on Pine Hollow hole one. Record those four values in docs/qa/r3/sound-device-detection.json in place of the nulls and in the README. If Lite is under 55 or Full under 40 on either phone, spend the remainder of the round on that phone's bottleneck with the resolution scaler and foliage budgets, never on desktop numbers.

Keep the physics and animation tests green, rerun docs/qa/overlap-audit.js at all six viewports, commit per piece with before and after screenshots, and record decisions in docs/decisions.md. Ask nothing.

---
